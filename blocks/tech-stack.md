# Block: Tech Stack (be explicit — else the agent picks inconsistently)  
  
- Language: <e.g. TypeScript (strict mode)>  
- HTTP framework: <e.g. Express / Fastify / Next.js route handlers>  
- DB + driver/ORM: <e.g. PostgreSQL + Drizzle>  
- Package manager / monorepo: <e.g. pnpm workspaces>  
- Runtime target: <e.g. Node 20>  
- Frontend (if any): <e.g. Next.js 15 App Router + Tailwind>  
  
Rule: pin the stack up front. Do not let the agent choose silently.
